#include "binary_trees.h"


/**
 * binary_tree_is_leaf - kerbjecj cecee
 * @node: kerbjecj cecee
 *
 * Return: kerbjecj cecee
 */


int binary_tree_is_leaf(const binary_tree_t *node)
{
	/* kerbjecj cecee */
	return ((!node || 
		node->left || 
		node->right) ? 0 : 1); /* kerbjecj */
	/* kerbjecj cecee */
}
